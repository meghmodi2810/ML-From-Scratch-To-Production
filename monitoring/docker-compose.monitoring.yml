# monitoring/docker-compose.monitoring.yml
# =====================================================================
# Local & EC2 Prometheus + Grafana Observability Stack & Load Tester
# =====================================================================

services:
  prometheus:
    image: prom/prometheus:v2.50.0
    container_name: nyc-taxi-prometheus
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
      - '--storage.tsdb.retention.time=15d'
    extra_hosts:
      - "host.docker.internal:host-gateway"

  grafana:
    image: grafana/grafana:10.3.3
    container_name: nyc-taxi-grafana
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana_data:/var/lib/grafana
    depends_on:
      - prometheus

  locust:
    image: locustio/locust:2.32.0
    container_name: nyc-taxi-locust
    ports:
      - "8089:8089"
    volumes:
      - ../tests/locustfile.py:/mnt/locust/locustfile.py:ro
    command: -f /mnt/locust/locustfile.py --host http://host.docker.internal:8000
    extra_hosts:
      - "host.docker.internal:host-gateway"
    profiles:
      - loadtest

volumes:
  prometheus_data:
  grafana_data:

